# IC-POT
Intent-Controlled Partial Optimal Transport (IC-POT): a lightweight Python library for partial optimal transport with pointwise, side-specific unmatched costs. It provides sparse and HiGHS-based solvers, configurable transport costs, and simple APIs for structured rejection policies.
